#lang racket

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; hum-parser: tests for hfile->htree
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

(require (only-in "../../../parser/functions/file.rkt" path->hfile)
         (only-in "../../../data-structures/hfile-to-htree.rkt" hfile->htree)
         test-engine/racket-tests)

; hfile->htree
()

(test)
